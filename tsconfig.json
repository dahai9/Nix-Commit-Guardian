{
  "compilerOptions": {
    // 输出配置
    "target": "ES2020",                     // 编译到 ES2020（现代 Node.js）
    "module": "commonjs",                   // 使用 CommonJS 模块系统
    "outDir": "./dist",                     // 输出目录
    "rootDir": "./src",                     // 源代码根目录
    
    // 模块解析
    "moduleResolution": "node",             // 使用 Node.js 风格的模块解析
    "esModuleInterop": true,                // 与 CommonJS 模块兼容
    "allowSyntheticDefaultImports": true,   // 允许合成默认导入
    
    // 类型检查
    "strict": true,                         // 启用所有严格检查
    "skipLibCheck": true,                   // 跳过库定义文件的类型检查
    "forceConsistentCasingInFileNames": true, // 强制文件名大小写一致
    
    // 声明文件
    "declaration": true,                    // 生成 .d.ts 声明文件
    "declarationMap": true,                 // 生成声明文件的 SourceMap
    "sourceMap": true,                      // 生成 SourceMap（便于调试）
    
    // 其他
    "resolveJsonModule": true,              // 允许导入 JSON 文件
    "lib": ["ES2020"]                       // 使用 ES2020 库定义
  },
  "include": ["src/**/*"],                  // 包含 src 目录下所有文件
  "exclude": ["node_modules", "dist"]      // 排除 node_modules 和 dist
}
